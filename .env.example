# Server Configuration
PORT=3001
SESSION_SECRET=your-random-session-secret-here

# App URL (required in production for OAuth callback)
# In development: http://localhost:3001
# In production: https://your-cloud-run-url.run.app
APP_URL=http://localhost:3001

# Google OAuth (get these from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Google Cloud Project ID (for Firestore)
GOOGLE_PROJECT_ID=your-google-project-id

# Anthropic API
ANTHROPIC_API_KEY=your-anthropic-api-key

# Frontend URL (only needed in development)
FRONTEND_URL=http://localhost:8080

# Allowed Emails (comma-separated, leave empty for no restrictions)
ALLOWED_EMAILS=phuong.smart@gmail.com

# Use Firestore (set to true in dev to test Firestore locally)
# In production, Firestore is used by default
# USE_FIRESTORE=true
